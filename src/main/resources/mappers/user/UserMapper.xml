<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.growtive.user.UserMapper">

    <resultMap id="UserMap" type="com.growtive.user.UserAccount">
        <id     column="id"            property="id"/>
        <result column="email"         property="email"/>
        <result column="display_name"  property="displayName"/>
        <result column="password" property="passwordHash"/>
        <result column="created_at"    property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.growtive.user.UserAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_account (email, display_name, password, created_at)
        VALUES (#{email}, #{displayName}, #{passwordHash}, NOW())
    </insert>

    <select id="findAll" resultMap="UserMap">
        SELECT id, email, display_name, password, created_at
        FROM user_account
        ORDER BY id DESC
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserMap">
        SELECT id, email, display_name, password, created_at
        FROM user_account
        WHERE email = #{email}
        LIMIT 1
    </select>

</mapper>
